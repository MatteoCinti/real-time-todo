type Todo {
  board: String!
  title: String!
  author: String
  description: String!
}

type Query {
  getTodos: [Todo]
}

type Mutation {
  createTodo(
    title: String!
    author: String!
    description: String!
    board: String!
  ): Todo!
}

type Subscription {
  todoCreated(board: String): Todo!
}
